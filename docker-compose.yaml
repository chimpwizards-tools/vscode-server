version: '3'

services:
  vscode:
    build: 
      context: .
      dockerfile: Dockerfile
    image: chimpwizardstools/vscode-fat-ide
    ports:
        - '8080:8080'   
    entrypoint: bash
    environment:
      PASSWORD: 'admin'
